# HELLA 組合商品上架後 — 雨刷查詢器對照稽核

日期：2026-08-12（台灣時間 13:40 左右執行）
範圍：Jerry 於 Cyberbiz 後台新增 78 個 HELLA 車種專屬組合商品後，驗證查詢器對照是否正確。

## 結論

對照本身沒有問題，而且今早 06:45（台灣）的排程已經自動把新商品上線了，不需要人工補推。
唯一要處理的是 **3 個商品標題文字**，都不影響查詢器選到的尺寸，但客人直接在商店逛到商品頁時看得到。

## 驗證方式與結果

| 檢查項目 | 結果 |
|---|---|
| Cyberbiz 抓到的 HELLA 商品 | 91（1 個單支自選 + 90 個車種組合）—— ✅ Jerry 核對後台：90 組合 + 1 單品，**數量一致，sitemap 這次沒漏** |
| 90 個組合是否全部被解析 | ✅ 全部（無「格式不符被略過」） |
| 解析後是否全部配到車 | ✅ 全部（無「配不到任何車」） |
| 組合價 vs 兩支單支加總 | ✅ 90 個全部相符，無多收 |
| BOSCH 是否被新商品影響 | ✅ 400 筆車款的 BOSCH 選項逐筆比對，0 筆變動（仍為 129 組合 / 216 單支） |
| GitHub Pages 供應的 data.json | ✅ 與本機重新建置的結果位元組相同，已是最新 |
| 商品頁 HTTP 狀態（抽查 6 個） | ✅ 全部 200 |
| 線上實際操作驗證 | ✅ CAMRY 三代、LEGACY 四代、S3、YARIS 兩個年段皆正確 |

上線後涵蓋率：HELLA 選項 289（組合 112 / 單支 177）；BOSCH 選項 345（組合 129 / 單支 216）。
沒有任何車款是 0 選項。

> 註：112 > 90，是因為「通用款」型的組合（例如 CR-V 第三代~第五代）會同時掛在多個年份上，這是預期行為。

## 要修的 3 個商品標題 —— ✅ 已於 2026-08-12 14:09 修完（大標＋SEO）

Jerry 授權後用 `~/projects/carmall-product-channel-shipping/fix_wiper_title_years.py` 改的。
before/after 快照：`snapshots/full_20260812-wipertitle-{before,after}.json`；還原用
`python3 fix_wiper_title_years.py --revert`。
驗證：拿 after 快照重跑 `--plan` 歸零；330 個商品逐欄比對，目標以外 **0 筆真實變動**
（唯一一筆差異是 57718820 的 `temperature_types` 從 `常溫` 變成 `room temperature`，
兩次 API 回應的語系顯示不同，不是本次寫入造成的 —— 這支腳本只對那 3 個 ID 送 title／seo_meta_tags）；
前台公開 JSON 已是新標題；handle、價格、SEO description／keywords 全部沒動。

改前先做過 parser 安全性驗證：把三個新標題灌進 `tools/wiper_products.json` 的副本重跑
`build_data.py`，產出的 `data.json` 與線上版位元組相同 —— 年份在比對前就被清掉，不影響查詢器。


### 1. TOYOTA YARIS —— 唯一一個會害客人買錯的

- 商品標題：`HELLA 三節式雨刷 (2支/組) 適用車型 TOYOTA YARIS【(2006~)24+14吋】`
- 問題：寫「2006~」等於宣稱 2014 以後的 YARIS 也適用，但 2014-2022 的 YARIS 是 **26+14 吋**，不是 24+14。
- 查詢器沒被騙 —— YARIS (14-22) 正確地給單支自選 26+14（$520），只有 YARIS (06-14) 給這個組合。
  風險在於客人不透過查詢器、直接在商店逛到這個商品頁。
- 建議：標題年份改成 `(2006~2014)`。

### 2. SUBARU LEGACY 第四代 —— 年份打反

- 商品標題：`…SUBARU LEGACY【第四代(19~15)26+18吋】`
- 應為 `(09~15)`。不影響比對（年份會被略過），純粹是客人看得到的錯字。

### 3. LUXGEN S3 —— 年份與查詢器對不上

- 商品標題寫 `第一代(12~15)`，查詢器（依 BOSCH 型錄）是 `(15-21)`。
- S3 在台灣是 2015 年上市，商品標題的年份比較可能是寫錯。尺寸 22+18 兩邊一致，比對正常。
- 建議：標題年份改成 `(2015~)` 或直接拿掉年份。

## 不用管的（已確認是正常現象）

- **世代編號與查詢器不同**：MAZDA 6 15-18（商品寫第四代／查詢器第三代）、TOYOTA RAV4 97-07（商品寫第二代／查詢器第一代）。
  年份與尺寸都一致，只是兩邊的世代數法不同。
- **開放式年份跨兩個年段**：X-TRAIL (2015~)、KONA (2019~)、LEXUS NX 第二代 (2018~)、SANTA FE 第四代 (2018~)。
  這些車款前後年段尺寸相同，掛在兩個年段上是對的。
- **56 個車款沒有 HELLA 選項**：因為需要 19/21/28 吋，而 HELLA 單支只做 14/16/18/20/22/24/26。
  這些車仍有 BOSCH，屬既有限制。

## BOSCH 的三個雙胞胎 —— ✅ 已於 2026-08-12 14:20 一併修完

修 HELLA 的時候發現，BOSCH 那條產品線的對應商品**帶著完全相同的三個年份錯誤**
（標題格式是照抄的，錯誤也一起抄過去了）。Jerry 追加授權後同一支腳本第二輪修掉：

| 商品 ID | 改動 |
|---|---|
| 68506471 | TOYOTA YARIS【(2006~)…】→【(2006~2014)…】（與 A 同樣的買錯風險，且 BOSCH 是主力線） |
| 68489390 | SUBARU LEGACY【第四代(19~15)…】→【第四代(09~15)…】 |
| 68507628 | LUXGEN S3【第一代(12~15)…】→【第一代(2015~)…】 |

大標前面的 `<p style=…>限量送除油膜膏</p>` 促銷 HTML 完整保留（子字串替換不會碰到）。
最終快照 `snapshots/full_20260812-wipertitle-final.json`；6 筆全部重跑 `--plan` 歸零；
前台公開 JSON 六筆都已是新標題。

## 順帶發現（與 HELLA 無關）

- LUXGEN S5 Hyper 在介面上會顯示成「S5 Hyper」＋年份標籤「Hyper (16-21)」，Hyper 出現兩次。純外觀，低優先。

## 稽核用的腳本

本次為一次性稽核，未寫入 repo。核心檢查（標題尺寸 vs 型錄同年份尺寸的交叉比對）值得未來重複使用時，
可以考慮併進 `tools/build_data.py` 的報表區。
